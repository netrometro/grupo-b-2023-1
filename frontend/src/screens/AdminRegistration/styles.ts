import { StyleSheet } from 'react-native';

const stylesAdminRegistration = StyleSheet.create({
  container: {
    width: '100%',
    alignItems: 'center',
  },
});

export default stylesAdminRegistration;
